rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // DEMO MODE: Allow any logged-in user to read/write everything.
    // Client-side filtering will be used for "privacy" in the UI.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
